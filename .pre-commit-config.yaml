repos:
    - repo: local
      hooks:
          - id: lint
            name: npm lint
            entry: bash -c "npm run lint:check"
            language: system
            pass_filenames: false

          - id: format
            name: npm format
            entry: bash -c "npm run format"
            language: system
            pass_filenames: false
